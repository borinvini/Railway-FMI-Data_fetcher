# Copy to .env and fill in. .env is git-ignored -- never commit real credentials.

# Email notifications (optional; pipeline skips them if unset)
FETCHER_SMTP_SERVER=smtp.gmail.com
FETCHER_SMTP_PORT=587
FETCHER_EMAIL_ADDRESS=you@example.com
FETCHER_EMAIL_PASSWORD=your-app-password-here
